startCommand:
  type: stdio
  build:
    dockerfile: ./Dockerfile
    dockerBuildPath: .
  configSchema:
    {}
  commandFunction: |
    (config) => ({
      "command": "./databricks-mcp-server",
      "env": {}
    })
